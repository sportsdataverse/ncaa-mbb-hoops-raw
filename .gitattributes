# Auto detect text files and perform LF normalization
* text=auto

# Raw capture bundles are gzipped binary — never LF-normalize (would corrupt bytes)
mbb/raw/** binary
mbb/**/*.json.gz binary
*.json.gz binary
